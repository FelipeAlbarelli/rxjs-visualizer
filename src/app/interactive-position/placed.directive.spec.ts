import { PlacedDirective } from './placed.directive';

describe('PlacedDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacedDirective();
    expect(directive).toBeTruthy();
  });
});
